// Minimal DataTables Buttons stub
(function($){
  if(!$ || !$.fn || !$.fn.DataTable) return;
  $.fn.dataTable = $.fn.dataTable || {};
  $.fn.dataTable.Buttons = function(table, opts){
    console.log('Buttons created', opts);
    this.table = table;
  };
})(window.jQuery);