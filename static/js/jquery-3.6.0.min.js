// Minimal jQuery 3.6.0 stub for offline use
(function(window, document){
  function $(arg){
    if(typeof arg==='function'){ if(document.readyState!='loading') arg(); else document.addEventListener('DOMContentLoaded',arg); return; }
    var nodes = typeof arg==='string'?document.querySelectorAll(arg):[arg];
    nodes.ready=function(cb){ if(document.readyState!='loading') cb(); else document.addEventListener('DOMContentLoaded',cb); };
    nodes.DataTable=function(opts){ console.log('DataTables stub called',opts); return {}; };
    return nodes;
  }
  window.jQuery=window.$=$;
})(window, document);